
# SunSmart DTH - MERN Stack Edition

This is the **full-stack version** of the Sri Thirumala Enterprises portal, featuring a persistent MongoDB database and a secure Express backend.

## 🚀 MERN Features

- **Persistent Database**: All orders and tickets are saved in MongoDB.
- **Secure AI Proxy**: Gemini API calls are made from the server to protect your credentials.
- **Unified Deployment**: The server handles both the API and serving the static React build.

## 🌐 Deployment (Render Web Service)

1. **Connect GitHub**: Select your repository.
2. **Environment Variables**:
   - `API_KEY`: Google Gemini Key.
   - `MONGODB_URI`: MongoDB Atlas Connection String.
3. **Build Settings**:
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

## 📦 Local Development

1. Ensure MongoDB is running locally.
2. `npm install`
3. Create `.env` with `API_KEY` and `MONGODB_URI`.
4. `npm run dev` (Frontend) and `npm run start` (Backend - optional, for full stack).

---
*Authorized Sun Direct Service Provider - Support: 9985265605*
